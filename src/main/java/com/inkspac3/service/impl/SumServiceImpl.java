package com.inkspac3.service.impl;

import com.inkspac3.entities.CustomArray;
import com.inkspac3.service.ArrSumService;
import org.apache.log4j.Logger;

public class ArrSumServiceImpl implements ArrSumService {
    private final Logger log = Logger.getLogger(this.getClass().getName());

    @Override
    public int calculateArrSumLength(CustomArray array) {
        return 0;
    }
}
