package com.inkspac3.service.impl;

import com.inkspac3.entities.CustomArray;
import com.inkspac3.service.ArrFindMinMaxService;
import org.apache.log4j.Logger;

public class ArrFindMinMaxServiceImpl implements ArrFindMinMaxService {
    private final Logger log = Logger.getLogger(this.getClass().getName());

    @Override
    public String getMin(CustomArray array) {
        return "";
    }

    @Override
    public String getMax(CustomArray array) {
        return "";
    }
}
