package com.inkspac3.service.impl;

import com.inkspac3.entity.CustomArray;
import com.inkspac3.service.ArrAvgValueService;
import org.apache.log4j.Logger;

public class ArrAvgValueServiceImpl implements ArrAvgValueService {
    private final Logger log = Logger.getLogger(this.getClass().getName());

    @Override
    public int calculateAvgLength(CustomArray array) {
        return 0;
    }
}
